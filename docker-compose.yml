version: "3.8"

services:
  stockquant:
    build: .
    container_name: stockquant
    ports:
      - "8000:8000"
    volumes:
      # 挂载数据目录（DB + WAL + SHM 文件在同一目录）
      - ./data:/app/data
    restart: unless-stopped
